# Large zip archives (re-download from Kaggle if needed)
*.zip

# Scraper progress file (ephemeral)
.pmu_scraper_progress.json

# Python
__pycache__/
*.pyc

# PMU data cache (large, not needed in repo)
pmu_programmes_cache/
httpsoffline.turfinfo.api.pmu.frrestclient7programme/
pmu_daily_json/
pmu_full_dataset.csv
pmu_data.csv

# Large derived parquet files (regeneratable from pmu_daily_json/ or pmu_data/)
pmu_dataset.parquet
pmu_features.parquet
pmu_course_raw_v2.parquet
pmu_dataset_v2.parquet
pmu_features_v2.parquet
pmu_feat_*.parquet
build_v2.log

# Secrets — NEVER commit
.env
.env.local
.env.*.local
*.key
*.pem
secrets/

# Supabase CLI temp state
supabase/.temp/
supabase/.branches/

# Large data directories (stored in Google Drive / Storage, not git)
pmu_data/
pmu_data_all.zip
pmu_data_gap.zip

# Scraper logs
scraper_async.log
scraper_monitor.log
